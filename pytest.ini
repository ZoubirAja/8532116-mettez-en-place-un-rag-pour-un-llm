[pytest]
# Ajoute la racine du repo à sys.path : les tests dans tests/ importent "utils.xxx" et
# "api.puls_events_api" comme s'ils étaient exécutés depuis la racine, même si pytest les
# découvre dans un sous-dossier.
pythonpath = .
